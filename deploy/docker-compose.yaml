services:
  detailing-crm-signature-pad-pwa-develop:
    image: ${ENV_DETAILING_CRM_FRONTEND_SIGNATURE_PAD_PWA_IMAGE}
    container_name: ${SIGNATURE_PAD_PWA_CONTAINER_NAME}
    ports:
      - '80:80'

    restart: always
    networks:
      - ${ENV_NETWORK}


networks:
  ${ENV_NETWORK}:
    external: true
